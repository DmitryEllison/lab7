package dimka.blinb.collection.interfaces;

/**
 * Общий тип команд
 *
 *      Будет использоваться как параметр, который может быть получен на стороне сервера
 * непосредственно из пользовательского интерфейса (UI)
 *      Все классы, реализующие ICommand, содержит данные – свойства и конструктор с установкой
 * их значений при инициализации – и больше ничего.
 */
public interface ICommand {
}
